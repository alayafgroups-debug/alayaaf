import { useState, type ReactNode } from "react";
import { createPortal } from "react-dom";
import { ChevronDown, X } from "lucide-react";
import { useI18n } from "@/i18n";
import { supabase } from "@/lib/supabaseClient";

type CreatedParty = { id: string; name: string; vatNumber: string; commercialRegistration: string; address: string };

type Props = { kind: "customer" | "vendor"; b2c?: boolean; onCreated: (party: CreatedParty) => void; onClose: () => void };

export default function PartyRegistrationDialog({ kind, b2c = false, onCreated, onClose }: Props) {
  const { t, direction } = useI18n();
  const [isIndividual, setIsIndividual] = useState(kind === "customer" && b2c);
  const [form, setForm] = useState({ name: "", phone: "", email: "", country: "المملكة العربية السعودية", taxMode: "not_registered", vat: "", cr: "", city: "", street: "", building: "", district: "", postal: "", invoiceRef: "", paymentTerms: "", license: "", licenseType: "" });
  const [saving, setSaving] = useState(false);
  const [error, setError] = useState("");
  const isCustomer = kind === "customer";
  const update = (key: keyof typeof form) => (value: string) => setForm((current) => ({ ...current, [key]: value }));
  const field = (label: string, key: keyof typeof form, required = false, type = "text") => <label className="space-y-1 text-xs text-slate-600"><span>{t(label)}{required ? " *" : ""}</span><input type={type} value={form[key]} onChange={(event) => update(key)(event.target.value)} placeholder={required ? t("مطلوب") : t("اختياري")} className="block h-10 w-full rounded border border-slate-200 px-3 text-sm text-slate-800" /></label>;
  const section = (title: string, children: ReactNode, open = false) => <details open={open} className="col-span-full rounded border border-slate-100"><summary className="flex cursor-pointer list-none items-center justify-between bg-slate-50 px-3 py-2 text-xs font-semibold text-slate-700">{t(title)}<ChevronDown className="h-4 w-4" /></summary><div className="grid grid-cols-1 gap-3 p-3 md:grid-cols-2">{children}</div></details>;
  const addressSection = section("العنوان اختياري", <>{field("المدينة", "city")}{field("الشارع", "street")}{field("رقم المبنى", "building")}{field("الحي", "district")}{field("الرمز البريدي", "postal")}</>);
  const billingSection = section("بيانات الفوترة اختياري", <>{field("المعرّف", "invoiceRef")}<label className="space-y-1 text-xs text-slate-600"><span>{t("شروط الدفع")}</span><select value={form.paymentTerms} onChange={(event) => update("paymentTerms")(event.target.value)} className="block h-10 w-full rounded border border-slate-200 bg-white px-3 text-sm"><option value="">{t("تحديد")}</option><option value="فوري">{t("فوري")}</option><option value="30 يوم">{t("30 يوم")}</option></select></label>{field("رقم الترخيص", "license")}<label className="space-y-1 text-xs text-slate-600"><span>{t("نوع ترخيص جهة الاتصال")}</span><select value={form.licenseType} onChange={(event) => update("licenseType")(event.target.value)} className="block h-10 w-full rounded border border-slate-200 bg-white px-3 text-sm"><option value="">{t("تحديد")}</option><option value="شركة">{t("شركة")}</option><option value="فرد">{t("فرد")}</option></select></label></>);

  const save = async () => {
    if (!form.name.trim()) { setError(t("الاسم مطلوب")); return; }
    if (!isIndividual && form.taxMode === "registered_sa" && !/^3\d{14}$/.test(form.vat)) { setError(t("أدخل رقم تسجيل ضريبي سعودي صحيح من 15 رقمًا")); return; }
    setSaving(true); setError("");
    const address = [form.building, form.street, form.district, form.city, form.postal].filter(Boolean).join("، ");
    const { data, error: saveError } = await supabase.from(isCustomer ? "customers" : "vendors").insert({
      id: crypto.randomUUID(), name: form.name.trim(), type: isIndividual ? "فرد" : isCustomer ? "شركة" : "مورد محلي", email: form.email.trim(), phone: form.phone.trim(), status: "نشط", opening_balance: "0", credit_limit: "0", country: isIndividual ? "" : form.country, tax_registration_mode: isIndividual ? "not_registered" : form.taxMode, tax_number: !isIndividual && form.taxMode === "registered_sa" ? form.vat.trim() : null, commercial_registration: !isIndividual ? form.cr.trim() || null : null, building_number: form.building.trim(), street: form.street.trim(), district: form.district.trim(), city: form.city.trim(), postal_code: form.postal.trim(), invoice_ref: form.invoiceRef.trim(), currency: "SAR", payment_terms: form.paymentTerms, license_number: form.license.trim(), business_type: form.licenseType,
    }).select("id, name, tax_number, commercial_registration, building_number, street, district, city, postal_code").single();
    setSaving(false);
    if (saveError || !data) { setError(saveError?.message ?? t("تعذر حفظ البيانات")); return; }
    onCreated({ id: String(data.id), name: String(data.name), vatNumber: String(data.tax_number ?? ""), commercialRegistration: String(data.commercial_registration ?? ""), address: [data.building_number, data.street, data.district, data.city, data.postal_code].map((value) => String(value ?? "")).filter(Boolean).join("، ") || address });
  };

  return createPortal(<div dir={direction} className="fixed inset-0 z-[100] flex items-center justify-center bg-slate-950/40 p-4" onMouseDown={onClose}><section className="max-h-[90vh] w-full max-w-3xl overflow-y-auto rounded-xl bg-white shadow-2xl" onMouseDown={(event) => event.stopPropagation()}><header className="flex items-center justify-between border-b border-slate-200 px-5 py-4"><div><p className="text-xs text-slate-400">{isCustomer ? t("العملاء") : t("الموردين")}</p><h2 className="text-lg font-bold text-slate-800">{t(isCustomer ? "إنشاء عميل جديد" : "إنشاء مورد جديد")}</h2></div><button onClick={onClose} className="rounded p-2 text-slate-500 hover:bg-slate-100"><X className="h-5 w-5" /></button></header><div className="grid grid-cols-1 gap-4 p-5 md:grid-cols-2">{isCustomer && <label className="col-span-full space-y-1 text-xs text-slate-600"><span>{t("نوع العميل")}</span><select value={isIndividual ? "individual" : "organization"} onChange={(event) => setIsIndividual(event.target.value === "individual")} className="block h-10 w-full rounded border border-slate-200 bg-white px-3 text-sm"><option value="individual">{t("شخص / عميل B2C")}</option><option value="organization">{t("جهة أو منشأة B2B")}</option></select></label>}{field(isIndividual ? "الاسم الشخصي" : isCustomer ? "اسم المنشأة" : "اسم المورد", "name", true)}{field("رقم الجوال", "phone")}{field("البريد الإلكتروني", "email", false, "email")}{!isIndividual && section("المنشأة والتسجيل الضريبي مطلوب", <><label className="space-y-1 text-xs text-slate-600"><span>{t("البلد")}</span><select value={form.country} onChange={(event) => update("country")(event.target.value)} className="block h-10 w-full rounded border border-slate-200 bg-white px-3 text-sm"><option value="المملكة العربية السعودية">{t("المملكة العربية السعودية")}</option><option value="">{t("اختياري")}</option></select></label><div className="space-y-2 text-xs text-slate-600"><span>{t("التسجيل في ضريبة القيمة المضافة")}</span><label className="flex items-center gap-2"><input type="radio" checked={form.taxMode === "not_registered"} onChange={() => update("taxMode")("not_registered")} />{t("غير مسجل في ضريبة القيمة المضافة")}</label><label className="flex items-center gap-2"><input type="radio" checked={form.taxMode === "registered_sa"} onChange={() => update("taxMode")("registered_sa")} />{t("جهة اتصال مسجلة في ضريبة القيمة المضافة في السعودية")}</label></div>{field("رقم التسجيل الضريبي", "vat")}{field("رقم السجل التجاري للعميل", "cr")}</>, true)}{addressSection}{billingSection}</div>{error && <p className="px-5 text-sm text-red-600">{error}</p>}<footer className="flex justify-end gap-2 border-t border-slate-100 px-5 py-4"><button onClick={onClose} className="rounded border border-slate-200 px-4 py-2 text-sm">{t("إلغاء")}</button><button onClick={() => void save()} disabled={saving} className="rounded bg-emerald-600 px-4 py-2 text-sm font-semibold text-white hover:bg-emerald-700 disabled:opacity-50">{saving ? t("جاري الحفظ...") : t("حفظ")}</button></footer></section></div>, document.body);
}
